# Timeblock 产品与交互设计

## 1. 产品定位

Timeblock 不是完整日历的替代品，而是一个“今晚怎么过”的快速决策工具。核心场景是用户下班到家后，在 15–30 秒内把接下来的 3–5 小时划成几个清晰、可执行的时间块。

核心承诺：**打开就看见今晚，想到就能放进去，变化时随手调整。**

## 2. 设计原则

1. **先有可用起点，再做精细编辑**：重复日程先自动生成，临时事项自动寻找空档。
2. **时间轴就是编辑器**：查看、移动、拉长和完成都在同一处发生，减少页面跳转。
3. **鼓励留白，不鼓励填满**：持续显示未安排时间；超过 75% 后逐级提醒计划密度。
4. **重复但不僵硬**：规则影响今天的初始计划，今天的移动和删除不反向破坏长期规则。
5. **失败要可恢复**：冲突时保留原位置；删除、暂停等动作及时反馈，不静默覆盖。

## 3. 信息架构

应用只保留两个一级入口：

- **今晚**：时间轴、快速添加、今晚会生效的重复日程、计划松弛度。
- **重复日程**：一周概览、规则列表、启停和新建规则。

设置和账户在 MVP 中降级处理。数据默认只保存在本机，避免首次使用被注册流程打断。

## 4. MVP 功能

| 功能 | 关键行为 | 用户价值 |
| --- | --- | --- |
| 今晚时间轴 | 默认展示 18:30–23:30；时间块按真实时长排布 | 一眼理解今晚的顺序和空档 |
| 快速添加 | 识别“时间 + 事项 + 时长”；缺少时间时自动找最近空档 | 几秒内完成排程，不填复杂表单 |
| 精确编辑 | 点击时间块修改标题、起止时间和颜色 | 需要时仍可精确控制 |
| 直接操控 | 上下拖动改变时间；底部拖动改变时长；5 分钟吸附 | 调整计划像移动便签一样自然 |
| 完成状态 | 时间块内直接勾选；可批量清理已完成项 | 规划和执行在同一界面闭环 |
| 冲突保护 | 重叠、越界或空档不足时阻止保存并说明原因 | 不会因为误操作破坏现有计划 |
| 重复日程 | 设置名称、星期、开始时间和时长；支持启停 | 常做的事无需每天重建 |
| 计划密度 | 显示已安排/留白时间，并给出三级松弛度文案 | 帮用户制定更能完成的计划 |
| 本地持久化 | 当天计划与规则写入浏览器；换日保留规则并生成新计划 | 刷新不丢失，又无需登录 |

## 5. 核心交互流程

### 5.1 打开应用

1. 直接进入“今晚”，不经过欢迎页。
2. 当天启用的重复规则自动成为时间块。
3. 顶部显示规划范围、已安排时间和留白时间。
4. 若当前时间在规划范围内，时间轴显示“现在”线和下一项提示。

### 5.2 快速安排一件事

用户可以输入 `19:00 吃晚饭 40分钟`：

- 同时有时间和时长：按输入精确创建。
- 只有事项：使用当前选中的 25/45/60 分钟，并寻找最近连续空档。
- 指定时间发生冲突：不擅自移动，提示该时间已有安排。
- 没有足够空档：提示先缩短或移动已有时间块。

晚餐、运动、阅读三个建议按钮用于高频场景的一击添加，同时自动分配易辨识的颜色。

### 5.3 调整和执行

- **拖动主体**：以 5 分钟为单位改变时间，越过边界会被限制。
- **拖动底部把手**：改变结束时间，最短 15 分钟。
- **点击主体**：打开精确编辑表单。
- **点击圆形勾选**：就地标记完成，不打开表单。
- **发生冲突**：时间块回到原位，并用轻提示解释原因。

颜色只承担快速区分作用，不让用户先维护复杂分类。

### 5.4 使用重复日程

1. 在“重复日程”查看未来一周每天会出现多少个规则。
2. 新建时只填写名称、开始时间、时长和星期。
3. 规则可以随时暂停；暂停不会删除已经生成的今天安排。
4. 用户也可以将某条启用规则临时加入今晚。

规则与当天实例分开保存，这是“规律但不僵硬”的关键：今天临时推迟运动，不会把以后所有运动都推迟。

## 6. 状态与边界

- **空计划**：时间轴仍完整展示，并用添加按钮和建议项引导第一步。
- **计划过满**：75% 以下为“刚刚好”，75%–90% 为“有点充实”，90% 以上提示删减。
- **跨日**：进入新的一天时保留重复规则，重新生成当天实例；旧实例不混入今天。
- **时间边界**：MVP 聚焦晚间，只允许 18:30–23:30。后续可让用户设置自己的晚间范围。
- **重复冲突**：自动生成只负责提供起点；新增或调整时仍走统一冲突校验。
- **存储不可用**：保留当前会话数据，并明确告知未能本地持久化。
- **移动端**：快速添加优先于时间轴；底部固定“今晚 / 添加 / 重复”三个操作。

## 7. 后续优先级

### P1：让每天规划更顺手

- 自定义“今晚”起止时间，并记住工作日/周末偏好。
- 撤销最近一次移动、删除或完成操作。
- 未完成事项在第二天首次打开时提供“带到今晚 / 放弃”选择。
- “低能量 / 正常 / 精力好”三档计划建议，自动调整推荐时长和留白。

### P2：连接真实日历

- 读取系统日历的忙碌时段，并以不可编辑底层区块展示。
- 可选通知：时间块开始前提醒，而不是为每次微调制造通知噪音。
- 多设备同步和账号体系；本地优先仍作为离线策略。

### 暂不进入 MVP

- 复杂项目、子任务、标签体系。
- 秒级专注计时器和统计报表。
- 自动把整天排满的 AI 计划。AI 更适合建议删减和寻找空档，而不是替用户决定所有事情。

## 8. 成功指标

- 首个时间块创建用时中位数低于 20 秒。
- 每次规划平均不超过 5 个时间块，且保留时间大于 15%。
- 创建重复规则后的 7 日内，至少 4 天直接使用或轻调自动日程。
- 因冲突导致的放弃率低于 5%。
- 用户完成规划后再次编辑的操作中，拖动调整占比高于表单编辑。
