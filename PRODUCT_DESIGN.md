# Timeblock 产品与交互设计

## 1. 产品定位

Timeblock 不是完整日历的替代品，而是一个“接下来怎么过”的快速决策工具。核心场景既包括下班后几小时，也包括周末全天：用户在 15–30 秒内把可用时间划成几个清晰、可执行的时间块。

核心承诺：**打开就看见当前日程，想到就能放进去，变化时随手调整。**

## 2. 设计原则

1. **先有可用起点，再做精细编辑**：重复日程按规则自动出现，常用事件内容在划选后直接复用。
2. **时间轴就是编辑器**：查看、移动、拉长和完成都在同一处发生，减少页面跳转。
3. **鼓励留白，不鼓励填满**：让时间轴上的空白自然表达余量，不用评价性文案制造压力。
4. **重复但不僵硬**：规则影响今天的初始计划，今天的移动和删除不反向破坏长期规则。
5. **失败要可恢复**：冲突时保留原位置；删除、暂停等动作及时反馈，不静默覆盖。
6. **功能可以丰富，界面必须克制**：使用单一强调色、弱边框和少量层级；常驻文字只保留日期、时间、行动与必要数据，图标操作提供可访问名称。

## 3. 信息架构

应用保留三个一级入口：

- **日程**：只包含日期控制、全宽 24 小时时间轴、1/3/7 日切换和划选后的事件内容选择。
- **重复日程**：一周概览，以及规则的新建、编辑、启停和删除。
- **管理**：常用内容管理、操作偏好，以及完整 JSON 备份的导出、导入和清空。

设置和账户在 MVP 中降级处理。数据默认只保存在本机，避免首次使用被注册流程打断。

## 4. MVP 功能

| 功能 | 关键行为 | 用户价值 |
| --- | --- | --- |
| 全天时间轴 | 覆盖 00:00–24:00，默认定位到当前时刻附近；手机端完整展开并随页面滚动 | 晚间与周末全天使用同一套交互 |
| 多日视图 | 切换 1 日、连续 3 日、周一开始的 7 日视图 | 按当天细排，也能概览周末或整周 |
| 划选创建 | 在空白时间轴上下滑动，15 分钟吸附；松手选择常用事件内容 | 先确定时间、再点一次内容，几秒完成排程 |
| 事件内容 | 新内容保存到数据中；分类和“加入常用”均可选 | 内容可复用，也不会把一次性事项塞进常用列表 |
| 常用内容管理 | “管理”页只展示常用内容；可新增、编辑、排序、归档和删除 | 临时事项不干扰管理，常用模板可随时整理 |
| 精确编辑 | 点击时间块修改标题、起止时间和颜色 | 需要时仍可精确控制 |
| 直接操控 | 上下拖动改变时间；底部拖动改变时长；5 分钟吸附 | 调整计划像移动便签一样自然 |
| 完成状态 | 时间块内直接勾选；可批量清理已完成项 | 规划和执行在同一界面闭环 |
| 冲突保护 | 重叠、越界或空档不足时阻止保存并说明原因 | 不会因为误操作破坏现有计划 |
| 重复日程 | 设置名称、分类、星期、时间和生效范围；支持编辑、启停和删除 | 常做的事无需每天重建，也不会留下难管理的副本 |
| 本地持久化 | 时间块按日期与规则一同写入浏览器；旧单日数据自动迁移 | 跨日期刷新不丢失，又无需登录 |
| 数据迁移 | 导出带版本号的完整 JSON；校验并确认后导入 | 在不同浏览器或平台之间手动迁移数据 |

## 5. 核心交互流程

### 5.1 打开应用

1. 直接进入“日程”，不经过欢迎页，默认使用上次选择的 1/3/7 日范围。
2. 可见日期的重复实例由规则与例外记录实时计算，不写入普通时间块数组。
3. 顶部只显示日期范围控制；其下只有全宽时间轴，不因屏幕宽度增加侧栏或下方卡片。
4. 若当前时间在规划范围内，时间轴显示无文字的“现在”线。

### 5.2 在时间轴划出一件事

1. 桌面端在空白时间直接拖动；移动端在空白格保持长按后进入划选，再上下拖动。界面实时显示起止时间和半透明预览块。
2. 范围向外吸附到 15 分钟；支持从下往上反向选择，最短为 15 分钟。
3. 划过已有安排时预览变为冲突态，松手后不创建并提示重新选择。
4. 在空白处松手后，显示已加入常用列表的事件内容及其可选分类。
5. 点一下内容立即创建；也可以在原选择框中新建事件内容。
6. 新建时只要求内容名称；分类和“加入常用”均可选。勾选时写入常用内容库，未勾选时只创建当前时间块。

“管理”页的“常用内容”面板只展示明确加入常用的内容。可新增、编辑名称/分类/颜色、调整顺序、移出常用或彻底删除；模板变化不改写已经创建的时间块。删除前说明影响并确认。新建一次性内容只写入当前时间块；从 V1 迁移来的旧非常用内容仍可保留在完整备份中。

移动端的 24 小时时间轴直接撑开页面，普通上下滑动始终滚动整个页面；手指在长按完成前开始移动会取消划选，避免滚动时误创建。长按进入划选后锁定当前页面位置，松手后恢复。已有时间块可点击进入精确编辑，并可修改所属日期。

### 5.3 调整和执行

- **拖动主体**：以 5 分钟为单位改变时间，越过边界会被限制。
- **拖动底部把手**：改变结束时间，最短 15 分钟。
- **点击主体**：打开精确编辑表单。
- **点击圆形勾选**：就地标记完成，不打开表单。
- **发生冲突**：时间块回到原位，并用轻提示解释原因。

颜色只承担快速区分作用，不让用户先维护复杂分类。

### 5.4 使用重复日程

1. 在“重复日程”查看本周每天会出现的规则，并新建或打开任一规则编辑。
2. 规则包含名称、可选分类、星期、开始时间、持续时长、生效日期和可选结束日期。
3. 暂停只阻止暂停区间内的新实例，已经发生的单次修改和完成记录保留；恢复后继续按原规则出现。
4. 删除规则前明确说明动态实例会消失，手动时间块不受影响。
5. 编辑重复实例时选择“仅这一次”会写入该日期的覆盖记录；选择“这一次及以后”会截止旧规则并创建新规则。删除使用同样的范围语义。

规则始终是重复实例的唯一来源；`recurrenceExceptions` 只保存单次修改、完成或取消。这是“规律但不僵硬”的关键：今天临时推迟运动不会改写过去，也不会制造重复副本。

### 5.5 迁移本地数据

1. “管理”页导出手动时间块、重复规则、例外记录、事件内容和偏好，文件包含格式标识和版本号。
2. 导入同时接受应用备份和原始 `localStorage` 状态；文件大小、结构、日期、时间和颜色都必须通过校验。
3. 校验成功后仍需用户确认才替换当前浏览器数据；取消或失败不会改变现有状态。

## 6. 状态与边界

- **空计划**：时间轴仍完整展示，用户直接在空白格划选第一项。
- **计划过满**：通过时间轴剩余空白直接判断，MVP 不追加评价性常驻文案。
- **跨日**：手动时间块按日期隔离；切换日期时保留旧安排，并实时推导该日期的重复实例。
- **重复来源**：切换日期时动态推导重复实例；只有发生单次编辑、完成或取消时才持久化例外。
- **时间边界**：每天可在 00:00–24:00 内安排；单个时间块仍不跨越午夜，跨夜事项拆到相邻两日。
- **范围切换**：1 日和 3 日从焦点日期开始；7 日自动对齐到周一，前后按钮按当前范围翻页。
- **重复冲突**：动态实例提供计划起点；新增或调整时仍走统一冲突校验。
- **存储不可用**：保留当前会话数据，并明确告知未能本地持久化；V1 状态自动迁移到 `timeblock-state-v2`。
- **导入失败**：格式或内容异常时拒绝导入并保留当前数据；单个文件限制为 2 MB。
- **全部终端**：“日程”只显示日期控制和完整时间轴，不重复展示规则、常用项或进度；桌面和平板内部滚动时间轴，移动端整列带动页面滚动。
- **移动端操作**：空白格长按后才进入划选；事件内容选择框固定在底部导航上方，三个图标入口均保留屏幕阅读器名称。
- **可读性**：正文以 16px 为基准，时间刻度不低于 12px，主要按钮与输入控件至少 44px；7 日概览只保留标题以避免信息拥挤。

## 7. V2 数据模型

```text
AppStateV2
├── settings { viewDayCount, snapMinutes }
├── blocksByDate               # 仅手动时间块
├── rules                      # 重复规则及生效/暂停范围
├── recurrenceExceptions       # 单日覆盖、完成或取消
└── eventContents              # 内容、分类、常用状态和顺序
```

旧 V1 中已经复制进 `blocksByDate` 的重复时间块会迁移成例外记录；普通时间块保持原日期与内容快照。导入同时兼容 V2、V1 备份和原始本地状态。

## 8. 后续优先级

### P1：让每天规划更顺手

- 未完成事项在第二天首次打开时提供“带到今天 / 放弃”选择。
- “低能量 / 正常 / 精力好”三档计划建议，自动调整推荐时长和留白。

### P2：连接真实日历

- 读取系统日历的忙碌时段，并以不可编辑底层区块展示。
- 可选通知：时间块开始前提醒，而不是为每次微调制造通知噪音。
- 多设备同步和账号体系；本地优先仍作为离线策略。

### 暂不进入 MVP

- 复杂项目、子任务、标签体系。
- 秒级专注计时器和统计报表。
- 自动把整天排满的 AI 计划。AI 更适合建议删减和寻找空档，而不是替用户决定所有事情。

## 9. 成功指标

- 首个时间块创建用时中位数低于 20 秒。
- 每次规划平均不超过 5 个时间块，且保留时间大于 15%。
- 创建重复规则后的 7 日内，至少 4 天直接使用或轻调自动日程。
- 因冲突导致的放弃率低于 5%。
- 用户完成规划后再次编辑的操作中，拖动调整占比高于表单编辑。
