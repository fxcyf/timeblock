# 项目经验

## 2026-08-30：移动端弹层避开底部导航

- **问题**：手机和 iPad 窄视口把编辑框、内容选择、多选工具栏和撤销反馈都集中在底部导航上方，让最高频的导航区域持续受到浮层干扰。
- **解决**：编辑表单改为四周留白的居中弹窗，内容选择器放到屏幕中上部并限制平板宽度，多选工具栏和反馈移到顶部工具栏下方；所有位置继续计算 iPadOS 安全区。
- **以后如何避免**：移动端底部导航属于高频保留区，新增临时界面优先使用居中弹窗或顶部上下文区域；响应式验收同时检查手机、iPad 窄屏的浮层边界与导航无遮挡。
- **关联提交**：`f8c3d9b`

## 2026-08-30：iPadOS 安全区与悬浮边缘

- **问题**：触屏窄视口的顶部工具栏会贴到视口顶边，底部导航固定在 `bottom: 0` 且只有上边框，在 iPadOS 圆角与 Home Indicator 区域显得拥挤并可能遮住 24:00。
- **解决**：启用 `viewport-fit=cover`，将顶部工具栏和底部导航统一放入安全区内并保留 8px 视觉间距；底部导航改为完整圆角边框，内容、选择栏、Toast 和内容弹层同步预留高度。
- **以后如何避免**：触屏固定元素必须同时验证安全区、视觉间距与内容避让；至少覆盖 390px、iPad 窄分屏和断点上限，并滚动到页面首尾检查粘性元素。
- **关联提交**：`de94b80`

## 2026-08-30：多选直接拖动与批量删除

- **问题**：时间块左侧粗色条形成过强的模板化视觉；多选只能通过日期/时间表单整体移动，也不能直接删除，打断了时间轴上的空间操作。
- **解决**：时间块改用对称细边框和柔和整块底色；多选模式支持拖动任意已选项，以抓取位置为锚点预览并原子移动整组，同时加入手动块与重复实例混合批量删除和一次撤销。
- **以后如何避免**：类型颜色应融入整体而非依赖固定装饰条；空间型批量操作优先保留在时间轴内，并同时验证任意锚点、跨日期、冲突回退、触摸手势和重复例外语义。
- **关联提交**：`11d8de5`

## 2026-08-30：完整交互、内容三态与触屏小时格

- **问题**：布尔 `favorite` 无法区分一次性内容与历史归档；重复表单的取消按钮会触发原生必填校验；短时间块在触屏纵轴上过窄；多个时间块只能逐个移动，且重复实例跨日期时容易失去规则归属；浏览器默认描边和小字号也降低了可用性。
- **解决**：保留 V2 本地状态并迁移为 `oneTime` / `favorite` / `archived` 三态，备份封装升级到 V3；重复表单改为仅保存时显式校验；手机和平板单日采用 24×4 小时格；新增带整组冲突预检的多选复制/移动和 `movedToDate` 重复例外；统一强调色、可读前景色、焦点环、点击区与 17px 基准字号。
- **以后如何避免**：互斥生命周期必须用显式状态而非叠加布尔值；取消/关闭控件一律声明 `type="button"` 并绕开保存校验；批量写入先用纯函数形成完整候选集再一次提交；触屏直接操控同时验证滚动意图、长按激活、午夜边界和多视口布局。
- **关联提交**：`e18bf2f`

## 2026-08-25：晚间规划原型

- **问题**：仓库只有项目骨架，产品范围容易扩张成完整日历，影响“下班后快速规划”这一核心场景。
- **解决**：将 MVP 收敛为“今晚时间轴 + 一句话添加 + 重复日程”，用独立排程领域层统一处理分钟、冲突、空档和规则实例化；同时制作响应式可运行原型和产品设计文档。
- **以后如何避免**：新增功能前先判断能否缩短首个时间块的创建时间；领域规则优先放入 `src/schedule.js` 并同步补测试，视觉交互用桌面和移动端浏览器各检查一次。
- **关联提交**：`6fc9fee`

## 2026-08-25：GitHub Pages 自动部署

- **问题**：用户无法访问开发服务器，需要一个无需维护服务器的固定公网地址。
- **解决**：利用原型的零构建静态架构，添加 GitHub Pages Actions 工作流；推送 `main` 后自动上传和发布整个站点。
- **以后如何避免**：静态资源继续使用相对路径以兼容项目子路径；部署后同时检查 Actions 结果、HTTP 状态和页面标题，不以“推送成功”代替线上验收。
- **关联提交**：`da7b1b9`

### 首次启用补充

- **问题**：在仓库设置中使用 GitHub 的 Static HTML 模板后，模板生成的工作流与已有工作流重复，会造成同一次推送触发两次部署。
- **解决**：保留 GitHub 生成且已与 Pages 设置关联的 `.github/workflows/static.yml`，删除旧的重复工作流并同步文档路径。
- **以后如何避免**：启用 Pages 前先确认仓库是否已有部署工作流；若使用 GitHub 模板，只保留一个拥有 `pages: write` 权限的发布入口。
- **关联提交**：`539607a`

## 2026-08-25：时间轴划选创建

- **问题**：表单优先的创建方式要求用户重复填写事项、开始和结束时间，没有发挥时间轴可直接操作的空间优势。
- **解决**：将主路径改为在空白时间轴划出范围，按 15 分钟吸附后选择高频行动立即创建；冲突时阻止创建，精确表单保留为自定义与键盘回退入口。
- **以后如何避免**：空间型界面的高频操作优先采用直接操控；手势功能必须同时用桌面指针和移动端触摸事件验收，并为刻度边界的亚像素误差保留测试。
- **关联提交**：`8c88590`

## 2026-08-25：简洁视觉收敛

- **问题**：大标题、多层卡片、彩色图标、阴影和浮动按钮同时出现，让辅助信息与核心时间轴争夺注意力。
- **解决**：统一为单一强调色、弱边框和无阴影视觉；隐藏装饰性模块，压缩重复日程标题区，并让移动端时间轴排在快速输入之前。
- **以后如何避免**：新增功能优先复用现有层级，不为每项功能创建新的视觉容器；桌面、移动端和次级页面必须一起做首屏视觉检查。
- **关联提交**：`18483da`

## 2026-08-25：常驻文字减量

- **问题**：即使颜色和卡片已经收敛，标题、状态句、说明段落和导航标签仍让首屏需要阅读，时间线不够直接。
- **解决**：常驻页面只保留日期、时间、行动与必要数据；用细进度线和图标代替进度文案、次要按钮与导航文字，同时为所有图标操作保留可访问名称。
- **以后如何避免**：新增常驻文字前先判断它是否直接帮助用户安排时间；操作提示尽量短且只出现一次，说明性内容放入按需打开的表单或文档。
- **关联提交**：`3b6f434`

## 2026-08-25：事件内容与常用列表

- **问题**：固定快捷输入和硬编码行动既占据首屏，也无法沉淀用户自己的事件内容；时间块只有标题，缺少可选的上一级分类。
- **解决**：移除快捷方式，建立独立事件内容库；划选时间后展示常用内容，新建内容时可选分类和“加入常用”，时间块保存内容引用与分类快照。
- **以后如何避免**：高频选择项应由用户使用历史和明确标记产生，不继续在界面中堆固定快捷入口；可复用模板与当天实例保持数据分离。
- **关联提交**：`decc9bb`

## 2026-08-28：全天多日时间轴

- **问题**：固定晚间窗口无法覆盖白天和周末，单日 `blocks` 状态也不能表达连续几天或整周的安排。
- **解决**：时间轴扩展为可滚动的 00:00–24:00，并加入 1 日、连续 3 日和周一开始的 7 日视图；时间块改为按日期保存，支持跨日期编辑，同时自动迁移旧版单日数据。
- **以后如何避免**：视图范围与领域数据保持分离，所有时间块操作必须显式携带日期；日期运算集中在 `src/calendar.js`，跨月、跨年、闰年和旧数据迁移都用自动化测试覆盖。
- **关联提交**：`5da3f5e`

## 2026-08-28：移动端长按划选

- **问题**：时间列使用即时拖动划选并禁用了原生触摸滚动，导致移动端只能从左侧狭窄的时间刻度区域滚动全天时间轴。
- **解决**：空白时间列恢复纵向原生滚动；手指保持 360ms 后才进入划选，提前移动超过容差则取消长按并继续滚动，划选期间锁定滚动位置并抑制系统长按菜单。
- **以后如何避免**：移动端的直接操控必须先区分滚动意图和编辑意图；手势阈值放入独立纯函数测试，并用真实触摸事件同时验证“快速滑动”和“长按后拖动”。
- **关联提交**：`c0ef8c2`

## 2026-08-28：事件内容概览与数据迁移

- **问题**：事件内容只能在划选后的弹层中看到，且浏览器 `localStorage` 没有跨设备迁移路径；直接导入任意 JSON 还可能破坏现有状态。
- **解决**：日程侧栏增加全部事件内容与分类概览；新增数据页导出带版本号的完整 JSON，并支持校验后的备份或原始状态导入，覆盖前必须确认，异常文件不改变现有数据。
- **以后如何避免**：可迁移状态必须有显式格式版本和纯函数校验；涉及覆盖本地数据的操作先校验、再确认、最后一次性替换，并用浏览器完成导出—清空—导入的端到端验证。
- **关联提交**：`9e3d734`

## 2026-08-28：移动端全页时间轴

- **问题**：移动端的 24 小时时间轴仍被限制在内部滚动容器中，滑到底后页面无法继续移动；日程页还重复展示了“重复”页已有的规则摘要，并让内容概览占用手机空间。
- **解决**：手机端隐藏规则摘要、内容概览和计划密度线，让完整时间轴直接撑开页面并使用文档级滚动；长按划选期间固定页面视觉位置，结束后恢复原滚动位置，桌面端继续使用内部滚动和内容侧栏。
- **以后如何避免**：移动端长内容优先确认页面级与组件级滚动的唯一归属；涉及长按编辑时同时用真实触摸事件验证快速滑动、长按锁定和松手恢复，并分别检查桌面和手机的响应式分支。
- **关联提交**：`a6a32b6`

## 2026-08-28：常用项管理

- **问题**：事件内容侧栏同时列出常用与一次性内容，临时事项会长期占据管理空间，而且常用模板无法直接修改分类等属性。
- **解决**：侧栏只渲染明确标记为常用的内容，并将每一行变为编辑入口；编辑器支持名称、分类、颜色和常用状态，移出常用不会删除底层数据，也不会改写已经创建的时间块快照。
- **以后如何避免**：区分“完整持久化数据”和“高频管理视图”，不要因为数据需要保留就默认把它常驻展示；模板编辑必须用稳定 ID 更新，并单独验证历史实例不被连带修改。
- **关联提交**：`bf66e98`

## 2026-08-28：全端纯时间轴日程页

- **问题**：辅助面板只在手机端隐藏，桌面和平板仍会把常用项等卡片排在时间轴侧面或下方；计划密度线也占据了日程首屏层级。
- **解决**：所有宽度的日程页统一只保留日期范围控制和一个全宽时间轴，删除进度组件和辅助面板；常用项管理移到“数据”页，桌面/平板内部滚动与手机页面级滚动分别保持不变。
- **以后如何避免**：响应式要求要覆盖桌面、平板、手机和用户截图对应尺寸，不把“移动端隐藏”误当成全端信息架构调整；首屏专注原则应直接落实到 DOM，而不只依赖断点隐藏。
- **关联提交**：`2e43638`

## 2026-08-29：V1 动态重复日程与管理闭环

- **问题**：旧实现把重复规则复制成普通时间块，规则编辑或删除后容易残留旧实例；常用内容只能移出常用而不能删除，且大量 8–11px 字号影响阅读。
- **解决**：将重复规则改为唯一来源，按日期动态推导实例，并用例外记录表达单次修改、完成和取消；“这一次及以后”通过截止旧规则并创建新规则实现。管理页补齐常用内容新增、排序、归档、删除、偏好、清空和 V2 备份，主要字号与点击区统一上调，并为可逆操作加入一次撤销。
- **以后如何避免**：重复数据只保留一个权威来源，实例差异单独建模；“移出列表”和“删除数据”必须使用不同操作与确认文案；视觉验收同时检查像素字号、点击高度和三种视口，而不只凭整体截图判断。
- **关联提交**：`24ecda2`

## 2026-09-20：移除时间块完成状态

- **问题**：时间块承担了任务完成追踪，界面中的勾选和批量清理与纯日程安排定位不一致，`done` 还渗透到重复例外、复制和备份数据。
- **解决**：移除完成按钮、完成样式与批量清理入口；状态迁移、重复实例和备份规范统一丢弃旧 `done` 字段，并保留旧数据/备份兼容导入。
- **以后如何避免**：删除领域能力时同时检查 UI、运行时状态、迁移、导入导出与批量操作，使用旧字段夹具验证兼容读取但不再写回。
- **关联提交**：`4a12b97`

## 2026-09-21：本地优先的 Supabase 云同步

- **问题**：纯 `localStorage` 无法跨设备延续安排，但把高权限密钥放进静态站点会暴露全部数据，整份状态直接覆盖也可能丢失另一台设备的离线修改。
- **解决**：使用可公开的 Supabase Publishable key 与邮箱认证，云表通过 RLS 按 `auth.uid()` 隔离；本机先落盘并自动推拉，检测到双端分歧时要求显式选择完整版本，离线和退出均保留本地数据。
- **以后如何避免**：静态客户端只使用公开密钥，secret/service-role 永不进入仓库；同步设计必须区分单侧更新与双侧分歧，并为认证刷新、RLS、离线和首次同步分别测试。
- **关联提交**：`747f761`

## 2026-09-21：邮箱确认回到应用路径

- **问题**：Supabase 邮箱确认本身成功，但完成后依赖项目默认 Site URL，GitHub Pages 项目站可能被导向不存在的根路径并显示 404。
- **解决**：注册请求通过 `redirect_to` 显式传入当前应用目录，确认回调自动恢复登录会话并进入管理页。
- **以后如何避免**：部署在子路径的静态应用不能只依赖认证服务的全局默认 URL；注册请求必须携带规范化应用目录，并用自动化测试检查编码后的回调地址。
- **关联提交**：`4f9cd9c`

## 2026-09-21：日程页手动云同步

- **问题**：自动同步缺少日程页内的可见入口，已安装的移动 Web App 中用户只能刷新整个页面或结束进程来主动检查云端修改。
- **解决**：登录后在固定日程工具栏显示立即同步图标，同步期间展示旋转与禁用状态，完成或失败给出反馈；检测到版本冲突时改为提醒状态并引导到管理页处理。
- **以后如何避免**：后台自动机制仍需提供可发现的手动触发与状态反馈；入口应放在用户当前工作的页面，并覆盖离线、繁忙和冲突状态。
- **关联提交**：`6268950`

## 2026-09-24：多选时间块总时长

- **问题**：多选上下文栏只显示项目数量，用户无法直接判断一组安排合计占用了多少时间。
- **解决**：按每个选中时间块的起止时间累加总分钟数，并在多选上下文栏随选择变化即时展示可读总时长；手动块、重复实例和跨日期选择使用同一计算方式。
- **以后如何避免**：批量选择摘要除数量外，还应优先呈现帮助用户决策的聚合数据；聚合计算保持为纯函数并覆盖空选择与跨日期项目测试。
- **关联提交**：`dde67b7`

## 2026-09-24：安排过程显示时长

- **问题**：创建时间块时只看到起止时间，用户需要自行换算当前划选占用的时长，触屏小时格在松手前甚至只有选区高亮。
- **解决**：统一生成“起止时间 · 时长”范围标签，在桌面划选框、触屏小时格选区和内容选择器中持续展示，并覆盖 `24:00` 边界。
- **以后如何避免**：涉及时间范围的创建流程应同时给出绝对时间和持续时间；桌面连续时间轴与触屏离散格两种渲染路径必须一起检查。
- **关联提交**：`537f332`
