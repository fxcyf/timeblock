# 测试指南

## 自动化测试

运行：

```bash
npm test
```

当前覆盖：

1. 24 小时时间解析与非法时间拒绝。
2. `19:00 吃晚饭 40分钟` 和无时间快速输入的解析。
3. 重叠冲突判断与相邻时间块放行。
4. 已有安排之间和末尾空档的寻找。
5. 重复规则按星期生效、停用和防止重复生成。
6. 共享星期的重复规则冲突与自动生成去重。
7. 时间轴划选的 15 分钟吸附、反向拖动、最短时长与晚间边界。
8. 浏览器亚像素落在刻度边缘时不多选一个时间段。
9. 分钟数转换为可读中文时长。

## 手动验收

先运行 `npm start`，在桌面和宽度小于 760px 的移动端视口各执行一次。

| 场景 | 操作 | 预期结果 |
| --- | --- | --- |
| 初次打开 | 打开首页 | 显示日期、今晚时间轴、默认规则实例和无文字的已安排比例线 |
| 划选创建 | 在 21:15 空白处按住并向下滑到 21:45 | 显示 21:15–21:45 预览；松手弹出快捷行动面板 |
| 一击选行动 | 在快捷行动面板点“安静阅读” | 立即创建对应颜色的时间块，不打开表单 |
| 反向划选 | 从较晚时间向上滑到较早时间 | 起止时间按时间先后显示并吸附到 15 分钟 |
| 划选冲突 | 从空白处划过一个已有时间块 | 预览显示冲突态；松手后不创建并给出提示 |
| 自定义行动 | 划选后点“换一个行动” | 精确表单预填刚刚选择的起止时间 |
| 快速添加 | 输入 `18:30 散步 20分钟` | 若时段空闲则创建；若被占用则提示冲突且不覆盖 |
| 自动找空档 | 只输入 `整理房间` | 按默认时长放入最早可用连续空档 |
| 调整默认时长 | 选择 25 分后添加无时长事项 | 新时间块持续 25 分钟 |
| 拖动 | 上下拖动时间块 | 以 5 分钟吸附；边界内成功保存 |
| 拖动冲突 | 将时间块拖到另一块上 | 区块回到原位并显示冲突提示 |
| 调整时长 | 拖动时间块底部把手 | 结束时间改变，最短保持 15 分钟 |
| 精确编辑 | 点击时间块，修改起止时间后保存 | 时间轴、进度和留白同步更新 |
| 完成 | 点击时间块右侧圆形按钮 | 原地变为完成态；清理按钮可将其移除 |
| 新建重复 | 进入重复日程并选择星期创建 | 规则出现在列表和一周概览，刷新后仍存在 |
| 启停规则 | 关闭某条规则开关 | 规则变为停用；已有今晚实例保持不变 |
| 响应式 | 在手机视口打开 | 时间轴先于快速输入，底部三个图标导航始终可操作 |
| 少文字视觉 | 检查桌面、手机和重复日程首屏 | 常驻文字仅保留日期、时间、行动和必要数据；无说明段落、重复标题或进度文案 |
| 刷新持久化 | 新增或移动后刷新页面 | 当天时间块和规则保持不变 |

## 可访问性检查

- 使用 Tab 可以到达导航、添加、完成、编辑和弹窗控件。
- 仅图标导航、添加和清理按钮都能被屏幕阅读器读出用途。
- 时间块聚焦后按 Enter 或空格可以打开编辑。
- 弹窗关闭后页面仍可继续操作；错误信息使用 `role="alert"`。
- 系统设置“减少动态效果”时不依赖动画表达状态。

## 线上部署检查

GitHub Pages 工作流完成后运行：

```bash
curl --fail --location https://fxcyf.github.io/timeblock/
```

响应应为 `200`，HTML 中应包含 `<title>Timeblock`。随后在无痕浏览器打开线上地址，确认样式、ECMAScript modules 和本地存储均正常工作。
