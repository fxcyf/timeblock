# 测试指南

## 自动化测试

运行：

```bash
npm test
```

当前覆盖：

1. 事件内容的规范化、三态迁移、常用/归档筛选、归档恢复、删除、分类颜色继承与排序。
2. 24 小时时间解析、重叠判断、相邻放行、空档寻找，以及 5/15/30 分钟划选所需的吸附边界。
3. 日期跨月、跨年、闰年运算，1/3/7 日范围与周一对齐。
4. 重复规则的生效日期、可选结束日期、星期与时间范围冲突。
5. 单次覆盖、跨日期移动、完成、取消，暂停时的历史例外，以及“这一次及以后”的规则拆分。
6. 旧单日状态、V1 `blocksByDate` 和已复制重复实例到 V2 例外记录的迁移。
7. V3 完整备份往返、V2/V1 备份兼容、原始状态导入、归档/移动字段和非法颜色回退。
8. 移动端长按前的手指移动容差与滚动取消判定。
9. 15 分钟小时格坐标、反向划选、午夜边界，以及跨小时视觉分片。
10. 多选跨日期移动、任意已选项拖动锚点、复制新 ID、重复实例脱离、组内复用、外部冲突和整组边界拒绝。
11. 重复表单只在保存时校验，以及关闭/取消按钮不提交表单。
12. 强调色持久化、自定义颜色规范化与自动前景色对比度。
13. `viewport-fit=cover`、iPadOS 安全区、顶部工具栏/底部导航的边缘间距，以及移动端浮层对底部导航区的避让。

## 手动验收

先运行 `npm start`，至少在 390px 手机、iPad 竖/横屏、900px 平板和 1440px 桌面各执行一次。

| 场景 | 操作 | 预期结果 |
| --- | --- | --- |
| 初次打开 | 打开首页 | 显示当前日期和 24 小时时间轴，并定位到当前时间附近 |
| 纯时间轴 | 查看桌面、平板与手机“日程” | 日期控制下只有全宽时间轴，无规则、常用内容或进度面板 |
| 全天边界与固定页头 | 到时间轴顶部和底部 | 可见 00:00 与 24:00；滚动到底后日期、翻页、视图和选择入口仍固定在顶部 |
| iPadOS 安全区 | 在 iPad 竖/横屏及窄分屏查看页面顶部和底部 | 顶部工具栏与底部导航均不贴屏幕边缘；完整上下边框可见，24:00 不被底部导航遮挡 |
| 移动端弹层位置 | 划选时间、编辑时间块或规则、进入多选并触发撤销反馈 | 编辑弹窗在屏幕中部显示；内容选择器位于中上部；多选工具栏和反馈位于顶部工具栏下方，底部导航区保持无遮挡 |
| 多日视图 | 依次切换 1 日、3 日、7 日 | 日期连续；7 日从周一开始并隐藏完成按钮和拖动把手 |
| 桌面划选 | 在空白处拖出约 30 分钟 | 按管理页设置的粒度吸附，松手显示常用内容 |
| 触屏单日格 | 在手机/iPad/900px 的 1 日视图查看和创建 | 显示 24 个小时行，每行四格；15/30 分钟事件分别占 1/2 格，跨小时事件连续分片 |
| 手机滚动 | 在空白时间列直接滑动 | 页面原生滚动，不出现选区 |
| 触屏长按 | 保持约 0.36 秒后跨小时或反向拖动 | 页面位置锁定并高亮 15 分钟格；松手恢复位置并打开内容选择 |
| 创建内容 | 新建名称，分类可留空，并选择是否保存为常用 | 当前时间块创建；只有勾选的内容会在下次选择和管理页出现 |
| 编辑手动时间块 | 点击后修改日期、时间、分类或颜色 | 无冲突时保存；移动、完成和删除后出现“撤销” |
| 多选时间块 | 点“选择”或长按已有时间块，再选多项 | 上下文栏显示数量，可 +15/+30、复制、删除或取消；不再通过编辑页移动 |
| 拖动整组 | 拖动任意一个已选时间块到另一时段或可见日期 | 显示保持日期差、时间差与时长的整组预览，松手后原子保存并可撤销 |
| 删除整组 | 多选手动块和重复实例后点“删除” | 手动块移除，重复实例只取消本次；一次撤销完整恢复 |
| 整组冲突 | 把多项复制/拖动到冲突或越界位置 | 预览变红并将整组放回原位，任何时间块都不保存 |
| 编辑重复实例 | 点击重复时间块并选择“仅这一次” | 只改变当前日期，未来日期继续使用原规则 |
| 修改未来重复 | 选择“这一次及以后”并调整时间 | 旧规则截止于前一天，新规则从当前实例日期开始 |
| 删除单次重复 | 选择“仅这一次”并删除 | 仅该日期实例消失，可撤销 |
| 删除未来重复 | 选择“这一次及以后”并删除 | 当前及以后实例消失，过去保留，可撤销 |
| 新建/编辑规则 | 在“重复”页设置名称、日期范围、星期、时间和颜色 | 规则及一周概览即时更新，冲突规则被拒绝 |
| 取消规则草稿 | 新建空表单、部分填写后分别点关闭、取消和 Escape | 始终退出且不新增规则/例外；只有点保存才显示字段错误并聚焦首项 |
| 暂停/恢复规则 | 关闭再打开规则开关 | 暂停区间不产生实例，恢复后继续生成，历史例外保留 |
| 删除规则 | 打开规则、点“删除规则”并确认 | 规则及其动态实例/例外删除，手动时间块不受影响，可撤销 |
| 管理常用内容 | 新增、编辑、上移/下移和归档 | 划选列表同步变化，已创建时间块保持快照；旧一次性内容不出现在归档区 |
| 恢复/删除归档 | 在“已归档”恢复或彻底删除 | 恢复后重新进入常用列表；删除需确认且可撤销，已有时间块不变 |
| 偏好与颜色 | 修改默认视图、时间吸附、强调色和自定义内容颜色 | 刷新后保持；文字对比清晰，危险色不跟随强调色变化 |
| 导出/导入 | 导出 JSON，再在另一浏览器导入并确认 | 时间块、规则、例外、内容、顺序和偏好完整恢复 |
| V1 迁移 | 使用旧版后刷新到新版 | 普通时间块保持；旧重复副本不重复出现，之后按动态规则工作 |
| 清空 | 在“管理”页清空全部数据并确认 | 数据清空；立即点“撤销”可恢复 |
| 字体、点击区与描边 | 检查四种宽度及键盘焦点 | 正文约 17px；桌面点击区至少 40px、触屏至少 44px；无异常黑边且焦点环清晰 |

## 可访问性检查

- Tab 可到达导航、日期控制、时间块、规则、管理操作和所有弹窗控件。
- 图标按钮均有可访问名称；时间块可用 Enter 或空格打开。
- 内容选择、新建内容、重复范围、分类、颜色和日期均可仅用键盘完成。
- 弹窗错误使用 `role="alert"`，反馈使用 `aria-live`；关闭弹窗后页面可继续操作。
- 系统设置“减少动态效果”时，不依赖动画表达状态。
- 系统高对比度模式下，控件、选中时间块和焦点仍有可辨边框。
- 时间块使用对称细边框与柔和整块底色，不出现仅位于左侧的分类色条。

## 线上部署检查

GitHub Pages 工作流完成后运行：

```bash
curl --fail --location https://fxcyf.github.io/timeblock/
```

响应应为 `200`，HTML 应包含 `<title>Timeblock`。随后用无痕浏览器确认静态资源、模块、V1→V2 迁移和本地持久化均正常。
