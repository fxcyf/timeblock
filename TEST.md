# 测试指南

## 自动化测试

运行：

```bash
npm test
```

当前覆盖：

1. 事件内容的规范化、可选分类、常用筛选、同分类颜色继承、重复拒绝、删除与跨非常用内容排序。
2. 24 小时时间解析、重叠判断、相邻放行、空档寻找，以及 5/15/30 分钟划选所需的吸附边界。
3. 日期跨月、跨年、闰年运算，1/3/7 日范围与周一对齐。
4. 重复规则的生效日期、可选结束日期、星期与时间范围冲突。
5. 单次覆盖、完成、取消，暂停时的历史例外，以及“这一次及以后”的规则拆分。
6. 旧单日状态、V1 `blocksByDate` 和已复制重复实例到 V2 例外记录的迁移。
7. V2 完整备份往返、V1 备份兼容、原始状态导入和异常数据拒绝。
8. 移动端长按前的手指移动容差与滚动取消判定。

## 手动验收

先运行 `npm start`，在桌面、平板和宽度小于 760px 的移动端各执行一次。

| 场景 | 操作 | 预期结果 |
| --- | --- | --- |
| 初次打开 | 打开首页 | 显示当前日期和 24 小时时间轴，并定位到当前时间附近 |
| 纯时间轴 | 查看桌面、平板与手机“日程” | 日期控制下只有全宽时间轴，无规则、常用内容或进度面板 |
| 全天边界 | 到时间轴顶部和底部 | 可见 00:00 与 24:00；手机端由整个页面滚动，桌面/平板由时间轴内部滚动 |
| 多日视图 | 依次切换 1 日、3 日、7 日 | 日期连续；7 日从周一开始并隐藏完成按钮和拖动把手 |
| 桌面划选 | 在空白处拖出约 30 分钟 | 按管理页设置的粒度吸附，松手显示常用内容 |
| 手机滚动 | 在空白时间列直接滑动 | 页面原生滚动，不出现选区 |
| 手机长按 | 保持约 0.36 秒后拖动 | 页面位置锁定并出现选区；松手恢复位置并打开内容选择 |
| 创建内容 | 新建名称，分类可留空，并选择是否保存为常用 | 当前时间块创建；只有勾选的内容会在下次选择和管理页出现 |
| 编辑手动时间块 | 点击后修改日期、时间、分类或颜色 | 无冲突时保存；移动、完成和删除后出现“撤销” |
| 编辑重复实例 | 点击重复时间块并选择“仅这一次” | 只改变当前日期，未来日期继续使用原规则 |
| 修改未来重复 | 选择“这一次及以后”并调整时间 | 旧规则截止于前一天，新规则从当前实例日期开始 |
| 删除单次重复 | 选择“仅这一次”并删除 | 仅该日期实例消失，可撤销 |
| 删除未来重复 | 选择“这一次及以后”并删除 | 当前及以后实例消失，过去保留，可撤销 |
| 新建/编辑规则 | 在“重复”页设置名称、日期范围、星期、时间和颜色 | 规则及一周概览即时更新，冲突规则被拒绝 |
| 暂停/恢复规则 | 关闭再打开规则开关 | 暂停区间不产生实例，恢复后继续生成，历史例外保留 |
| 删除规则 | 打开规则、点“删除规则”并确认 | 规则及其动态实例/例外删除，手动时间块不受影响，可撤销 |
| 管理常用内容 | 新增、编辑、上移/下移、取消常用 | 划选列表同步变化，已经创建的时间块保持快照 |
| 删除常用内容 | 编辑常用内容、点删除并确认 | 模板从管理与划选列表消失，已有时间块不变，可撤销 |
| 偏好 | 修改默认视图和时间吸附 | 刷新后保持；新划选采用 5/15/30 分钟粒度 |
| 导出/导入 | 导出 JSON，再在另一浏览器导入并确认 | 时间块、规则、例外、内容、顺序和偏好完整恢复 |
| V1 迁移 | 使用旧版后刷新到新版 | 普通时间块保持；旧重复副本不重复出现，之后按动态规则工作 |
| 清空 | 在“管理”页清空全部数据并确认 | 数据清空；立即点“撤销”可恢复 |
| 字体与点击区 | 检查三种宽度 | 正文约 16px，时间刻度清晰，主要输入与按钮点击高度至少 44px |

## 可访问性检查

- Tab 可到达导航、日期控制、时间块、规则、管理操作和所有弹窗控件。
- 图标按钮均有可访问名称；时间块可用 Enter 或空格打开。
- 内容选择、新建内容、重复范围、分类、颜色和日期均可仅用键盘完成。
- 弹窗错误使用 `role="alert"`，反馈使用 `aria-live`；关闭弹窗后页面可继续操作。
- 系统设置“减少动态效果”时，不依赖动画表达状态。

## 线上部署检查

GitHub Pages 工作流完成后运行：

```bash
curl --fail --location https://fxcyf.github.io/timeblock/
```

响应应为 `200`，HTML 应包含 `<title>Timeblock`。随后用无痕浏览器确认静态资源、模块、V1→V2 迁移和本地持久化均正常。
