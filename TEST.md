# 测试指南

## 自动化测试

运行：

```bash
npm test
```

当前覆盖：

1. 事件内容名称与可选分类的规范化、常用标记和重复内容复用。
2. 常用内容筛选、分类去重与同分类颜色继承。
3. 常用内容编辑时的身份保留、文本规范化和重复内容拒绝。
4. 24 小时时间解析与非法时间拒绝。
5. 重叠冲突判断与相邻时间块放行。
6. 已有安排之间和末尾空档的寻找。
7. 重复规则按星期生效、停用和防止重复生成。
8. 共享星期的重复规则冲突与自动生成去重。
9. 时间轴划选的 15 分钟吸附、反向拖动、最短时长与全天边界。
10. 浏览器亚像素落在刻度边缘时不多选一个时间段。
11. 分钟数转换为可读中文时长。
12. 日期跨月、跨年和闰年运算，1/3/7 日范围及周一对齐。
13. 旧版单日 `blocks` 到按日期 `blocksByDate` 的存储迁移。
14. 长按开始前的手指移动容差与滚动取消判定。
15. 完整备份往返、原始 `localStorage` 状态兼容导入和异常数据拒绝。

## 手动验收

先运行 `npm start`，在桌面和宽度小于 760px 的移动端视口各执行一次。

| 场景 | 操作 | 预期结果 |
| --- | --- | --- |
| 初次打开 | 打开首页 | 显示当前日期、24 小时时间轴、默认规则实例，并滚到当前时刻附近 |
| 全天边界 | 滚动到时间轴顶部和底部 | 可看到 00:00 与 24:00，并能在首尾各划出 15 分钟时间块；手机端到达底部时页面也到达末尾 |
| 三日视图 | 点顶部“3”并前后翻页 | 同屏出现连续三天；翻页后日期连续且可分别划选 |
| 周视图 | 点顶部“7” | 同屏出现周一到周日，跨月或跨年时日期仍正确 |
| 返回今天 | 翻到其他日期后点“今” | 回到包含今天的范围，并重新定位当前时刻 |
| 桌面划选 | 用鼠标在 21:15 空白处拖到 21:45 | 显示 21:15–21:45 预览；松开弹出事件内容面板 |
| 移动端滚动 | 从时间轴任意空白列直接上下滑动 | 整个页面跟随手指滚动，时间轴内部滚动位置保持为 0，且不出现选区 |
| 移动端长按划选 | 在 21:15 空白格保持约 0.4 秒，再拖到 21:45 | 长按后页面位置锁定并出现选区和轻触觉反馈；松手恢复原位置并弹出事件内容面板 |
| 长按误触取消 | 手指落下后立刻上下移动 | 识别为滚动，不创建 15 分钟选区 |
| 复用常用内容 | 在事件内容面板点“安静阅读” | 立即创建对应颜色的时间块，不打开表单 |
| 反向划选 | 从较晚时间向上滑到较早时间 | 起止时间按时间先后显示并吸附到 15 分钟 |
| 划选冲突 | 从空白处划过一个已有时间块 | 预览显示冲突态；松手后不创建并给出提示 |
| 新建常用内容 | 划选后点加号，填写“拉伸”、分类“健康”并勾选加入常用 | 创建带“健康”分类的时间块；下一次划选时列表出现“拉伸” |
| 新建一次性内容 | 划选后新建“泡茶”且不勾选加入常用 | 创建并保存内容；下一次划选时常用列表不出现“泡茶” |
| 可选分类 | 新建内容时分类留空 | 时间块正常创建，不显示空分类占位 |
| 常用项面板 | 在桌面端查看日程右侧“常用项”面板 | 只显示已加入常用的内容及分类，非常用内容不出现 |
| 编辑常用项 | 点击常用项，修改名称、分类和颜色后保存 | 面板与下一次划选列表同步更新；已经创建的时间块保持原内容 |
| 移出常用 | 编辑常用项时取消“保留为常用”并保存 | 该项从面板和划选列表消失，但导出的完整数据中仍保留 |
| 拖动 | 上下拖动时间块 | 以 5 分钟吸附；边界内成功保存 |
| 拖动冲突 | 将时间块拖到另一块上 | 区块回到原位并显示冲突提示 |
| 调整时长 | 拖动时间块底部把手 | 结束时间改变，最短保持 15 分钟 |
| 精确编辑 | 点击时间块，修改日期或起止时间后保存 | 时间块移动到目标日期，时间轴与进度同步更新 |
| 完成 | 点击时间块右侧圆形按钮 | 原地变为完成态；清理按钮可将其移除 |
| 新建重复 | 进入重复日程并选择星期创建 | 规则出现在列表和一周概览，刷新后仍存在 |
| 启停规则 | 关闭某条规则开关 | 规则变为停用；已经生成的日期实例保持不变 |
| 日程信息去重 | 分别查看“日程”和“重复” | 日程页不再显示重复规则摘要，规则只在“重复”页集中展示 |
| 响应式 | 在手机视口打开 | 日程页只保留日期控制、完整时间轴和底部导航；内容概览不占用首屏，选择框与新建表单位于底部导航上方 |
| 少文字视觉 | 检查桌面、手机和重复日程首屏 | 常驻文字仅保留日期、时间、行动和必要数据；无说明段落、重复标题或进度文案 |
| 刷新持久化 | 新增常用内容、切换范围或移动时间块后刷新 | 各日期时间块、1/3/7 日偏好、规则、事件内容和常用标记保持不变 |
| 导出数据 | 进入“数据”并点击导出 | 下载 `.json` 文件，包含格式版本、时间块、规则、事件内容和视图偏好 |
| 导入备份 | 在另一浏览器选择导出的 JSON 并确认 | 当前本地数据被备份内容替换，刷新后保持一致 |
| 取消导入 | 选择有效备份后在覆盖确认中取消 | 当前本地数据不改变 |
| 异常导入 | 选择非 JSON、结构错误或超过 2 MB 的文件 | 拒绝导入、显示原因，当前本地数据不改变 |

## 可访问性检查

- 使用 Tab 可以到达导航、添加、完成、编辑和弹窗控件。
- 仅图标导航、添加和清理按钮都能被屏幕阅读器读出用途。
- 事件内容选择、新建内容、常用项编辑、可选分类和常用勾选均可用键盘完成。
- 时间块聚焦后按 Enter 或空格可以打开编辑。
- 弹窗关闭后页面仍可继续操作；错误信息使用 `role="alert"`。
- 系统设置“减少动态效果”时不依赖动画表达状态。

## 线上部署检查

GitHub Pages 工作流完成后运行：

```bash
curl --fail --location https://fxcyf.github.io/timeblock/
```

响应应为 `200`，HTML 中应包含 `<title>Timeblock`。随后在无痕浏览器打开线上地址，确认样式、ECMAScript modules 和本地存储均正常工作。
